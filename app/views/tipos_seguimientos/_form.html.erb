<%= form_with(model: tipos_seguimiento, local: true) do |form| %>
  <% if tipos_seguimiento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipos_seguimiento.errors.count, "error") %> prohibited this tipos_seguimiento from being saved:</h2>

      <ul>
      <% tipos_seguimiento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_field :comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
