<%= form_with(model: type_request, local: true) do |form| %>
  <% if type_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_request.errors.count, "error") %> prohibited this type_request from being saved:</h2>

      <ul>
      <% type_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :request_id %>
    <%= form.text_field :request_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
